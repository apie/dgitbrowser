def get_repo_file_listing(path):
    # TODO implement
    # make sure we can not traverse outside of the allowed paths
    # if repo base path not in allowed base paths (a file with pathnames or a dir containing the checkouts)
    return ['README.md', 'test.txt']